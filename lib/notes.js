// const fs = require('fs');
// const path = require('path');
// // add to the note array
// const createNote = (note,noteArray) => {
//     noteArray.push(note)

//     // writes notes to db.json 
//     fs.writeFileSync(       
//         path.join(__dirname, './db/db.json'), //issue recognizing db.json file
//         JSON.stringify({notes: noteArray }, null, 2) 
//     );
// };

// const editNote = (editNote, noteArray) => {
//     const index = noteArray.findIndex(note => note.id === editedNote.id);
// };


// const findId = (id, noteArray) => {

// };

// const deleteNote = (note, noteArray) => {

// };